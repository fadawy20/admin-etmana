import { SallerconvertPipe } from './sallerconvert.pipe';

describe('SallerconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new SallerconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
